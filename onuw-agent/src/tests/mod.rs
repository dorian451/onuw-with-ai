// use crate::agent::Agent;
// use onuw_game::game::ONUWGame;
// use std::sync::Arc;

// fn players(num: i32) -> Vec<Arc<Agent>> {
//     (0..num).map(|i| Arc::new(Agent::new().unwrap())).collect()
// }

// #[tokio::test]
// async fn test() {}
